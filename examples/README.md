# Examples

- CLI quickstart:
  - `vectorless-rag index-and-query --docs examples/data --query "natural language" --top-k 5`
- Notebook demo:
  - Open `examples/vectorless_rag_demo.ipynb` and run all cells.

The `examples/data/` folder contains a few small `.txt` files to get started.
