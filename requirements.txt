# Core dependencies
# Pin NLTK to <4 for stability across tokenizer resource changes
nltk>=3.8,<4
pyyaml>=6.0.1

# Optional: For advanced reranking (cross-encoder models)
# sentence-transformers>=2.2.2

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.1.0
mypy>=1.5.0
